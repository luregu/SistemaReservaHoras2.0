/**
 * Extrae el rut del string entregado, sin digito verificador.
 * @constructor
 * @param {string} rut - rut entregado
 */
export declare function extractRut(rut: string): string;
/**
 * Extrae el digito verificado del string entregado
 * @constructor
 * @param {string} rut - rut entregado
 */
export declare function extractDv(rut: string): string;
/**
 * Valida el formato del RUT entregado
 * @constructor
 * @param {string} rut - rut entregado
 */
export declare function isFormatoRutValido(rut: string): boolean;
/**
 * Valida que el RUT entregado sea valido
 * @constructor
 * @param {string} rut - rut entregado
 */
export declare function isRutValido(rut: string): boolean;
/**
 * Valida que el digito verificador entregado es el correcto
 * @constructor
 * @param {string} rut - rut entregado
 */
export declare function isDvValido(rut: string): boolean;
/**
 * Elimina puntos, espacios y guiones del rut entregado con valores en mayuscula
 * @constructor
 * @param {string} rut - rut entregado
 */
export declare function getCleanRut(rut: string): string;
/**
 * Entrega el digito verificador que corresponde al rut entregado sin digito verificador
 * @constructor
 * @param {string} rut_sin_dv - rut sin dv
 */
export declare function getValidDV(rut_sin_dv: string): string;
/**
 * Entrega el RUT entregado con puntos y guiones
 * @constructor
 * @param {string} rut - rut entregado
 */
export declare function getRutFormato(rut: string): string;
/**
 * Entrega los distintos caracteristicas del RUT entregado
 * @constructor
 * @param {string} rut_con_dv - rut entregado
 */
export declare function getRutStatus(rut_con_dv: string): {
    rut: string;
    dv: string;
    formato: string;
    isRutValido: boolean;
    isDvValido: boolean;
    isFormatoValido: boolean;
};
